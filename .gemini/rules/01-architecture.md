# Architecture Rules

## 1. ディレクトリ構造の制約
- `src/features/` : ドメインごとの機能モジュール。他機能への依存は禁止。
- `src/shared/` : 複数機能で使い回す共通コンポーネント。

## 2. エラーハンドリング
- エラーは絶対に握りつぶさず、必ず `AppError` クラスを経由してスローすること。
- エラーメッセージには、デバッグに必要なコンテキスト（関数名、引数）を含めること。
